# FreqUI 前端多语言接入说明

## 当前状态

本目录基于官方 `freqtrade/frequi`，已接入 `vue-i18n`，目前提供：

- 简体中文（`zh-CN`，默认语言）
- English（`en`）
- 顶部导航和移动端菜单中的语言切换
- 浏览器语言自动识别（中文浏览器默认中文）
- 使用浏览器 `localStorage` 持久化选择，键名为 `ftUILocale`

## 已覆盖页面

- 登录页：机器人名称、API 地址、用户名、密码、错误提示和按钮
- 首页：欢迎语、帮助链接和说明
- 顶部导航、移动端菜单、底部导航
- 仪表盘：收益、持仓、已平仓、钱包和图表模块标题及说明
- 设置页：界面、图表、通知和回测设置
- 交易控制按钮：开始、停止、暂停、重载配置、强制退出/开仓及确认对话框

## 主要文件

- `src/locales/index.ts`：i18n 实例、语言检测和持久化切换
- `src/locales/zh-CN.ts`：简体中文资源
- `src/locales/en.ts`：英文资源
- `src/components/LanguageSelect.vue`：语言选择组件
- `src/main.ts`：注册 i18n 插件

后续翻译页面只需在两个语言资源文件中增加相同的键，并在 Vue 模板中使用 `$t('...')`，或在 `<script setup>` 中使用 `useI18n()` 的 `t()`。

## 本地开发和构建

```bash
cd /Users/leeee/project/frequi-zh
pnpm install
pnpm run dev
```

生产构建：

```bash
pnpm run typecheck
pnpm run lint-ci
pnpm run build
```

构建产物位于 `dist/`。部署时可以用独立的 Nginx/静态站点托管 `dist/`，也可以在 Freqtrade 镜像构建阶段将 `dist/` 复制到 UI 静态资源目录。不要直接把翻译代码写入 Freqtrade 运行时生成的 `freqtrade/rpc/api_server/ui/installed`，因为重新安装或升级 UI 时该目录可能被覆盖。

## 后续工作

当前是第一阶段核心路径翻译，交易详情、回测结果、图表 tooltip、日志和分析页面仍有不少英文硬编码文本。后续可按页面逐步迁移，优先处理交易和回测流程中的按钮、表头、状态和错误提示。
