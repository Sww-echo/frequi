# FreqUI 前端多语言接入说明

## 当前状态

本目录基于官方 `freqtrade/frequi`，已接入 `vue-i18n`，目前提供：

- 简体中文（`zh-CN`，默认语言）
- English（`en`）
- 顶部导航和移动端菜单中的语言切换
- 浏览器语言自动识别（中文浏览器默认中文）
- 使用浏览器 `localStorage` 持久化选择，键名为 `ftUILocale`

## 已覆盖页面

- 登录页：机器人名称、API 地址、用户名、密码、错误提示和按钮
- 首页：欢迎语、帮助链接和说明
- 顶部导航、移动端菜单、底部导航
- 仪表盘：收益、持仓、已平仓、钱包和图表模块标题及说明
- 设置页：界面、图表、通知和回测设置
- 交易控制按钮：开始、停止、暂停、重载配置、强制退出/开仓及确认对话框
- 交易页：交易列表、交易详情、订单、止损、资金费率、强制开仓/平仓和自定义数据
- 回测页：参数配置、历史结果、运行状态、汇总、对比、逐周期明细和结果分析
- 数据与分析页：数据下载、Lookahead Analysis、Recursive Analysis、Pairlist 配置和策略参数
- 图表：K 线、余额、收益、市场变化、交易日志、交易时长、钱包历史及 tooltip 文案
- 机器人及通用组件：机器人列表、状态、重命名、主题、复制、编辑、确认弹窗、分页和错误提示

语言资源目前包含 685 个叶子键，`en.ts` 和 `zh-CN.ts` 的键结构一致。页面中保留的英文主要是交易所、策略、交易对、API 字段、指标缩写等专有名词或后端返回值。

## 主要文件

- `src/locales/index.ts`：i18n 实例、语言检测和持久化切换
- `src/locales/zh-CN.ts`：简体中文资源
- `src/locales/en.ts`：英文资源
- `src/components/LanguageSelect.vue`：语言选择组件
- `src/main.ts`：注册 i18n 插件

后续翻译页面只需在两个语言资源文件中增加相同的键，并在 Vue 模板中使用 `$t('...')`，或在 `<script setup>` 中使用 `useI18n()` 的 `t()`。

## 本地开发和构建

```bash
cd /root/frequi
pnpm install
pnpm run dev
```

生产构建：

```bash
pnpm run typecheck
pnpm run lint-ci
pnpm run build
```

构建产物位于 `dist/`。当前服务器使用 Docker 多阶段构建和 Nginx 静态托管，前端容器只监听宿主机 `127.0.0.1:3001`，由宿主机 Nginx 反代到 `tracker.codingsww.com`。Freqtrade API 容器监听 `127.0.0.1:8080`，前端通过 `/api/` 访问该 API。机器人当前使用 Gate 模拟盘、`SampleStrategy` 和 5 分钟周期，配置 `initial_state: running`，容器重启后会自动恢复行情采集。

服务器更新部署：

```bash
cd /root/frequi
docker compose build frequi
docker compose up -d frequi
curl -sS https://tracker.codingsww.com/api/v1/ping
```

预期 API 响应为 `{"status":"pong"}`。不要直接把翻译代码写入 Freqtrade 运行时生成的 `freqtrade/rpc/api_server/ui/installed`，因为重新安装或升级 UI 时该目录可能被覆盖。

## 验证状态

- `docker compose build frequi`：通过，Vite 生产构建成功。
- `docker compose up -d frequi`：通过，容器正常启动。
- Freqtrade 重建后自动进入 `RUNNING`，并确认 `dry_run=true`。
- `https://tracker.codingsww.com/`：HTTP 200。
- `https://tracker.codingsww.com/api/v1/ping`：HTTP 200，返回 `{"status":"pong"}`。
- `BTC/USDT` 的 `5m` K 线接口可返回数据；当前交易数为 0，因为 `SampleStrategy` 尚未产生入场信号。
- 静态键审计：中英文各 685 个叶子键，未发现缺失翻译键。
